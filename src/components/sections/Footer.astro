---
import { MapPin, Mail, Phone } from "lucide-react";
import { t } from "../../i18n/utils";
import { getRouteHref } from "../../i18n/routes";
import { STORE_CATALOG_LIVE } from "../../config/storeFlags";
import AsiavenLogo from "../ui/AsiavenLogo.astro";

const lang = Astro.currentLocale === "en" ? "en" : "es";
const currentYear = new Date().getFullYear();

// Footer 100% corporativo — ningún enlace apunta a /store salvo el único
// "enlace puente" hacia la Tienda (columna "Enlaces Rápidos"). El equivalente
// para la Tienda vive en src/components/store/StoreFooter.astro.

// lucide-react@1.24.0 (instalado en este proyecto) ya no incluye íconos de
// marca (LinkedIn/Instagram/TikTok/Facebook); se usan trazos de línea
// genéricos equivalentes en su lugar. Se retiró Twitter/X: el cliente no
// proporcionó un handle real para esa red, y dejar un enlace "#" muerto
// mezclado con las demás (ya todas reales) sería inconsistente.
const socialLinks = [
  {
    label: "LinkedIn",
    href: "https://www.linkedin.com/company/grupo-asiaven/",
    path: "M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z M2 9h4v12H2z M4 6a2 2 0 1 0 0-4 2 2 0 0 0 0 4z",
  },
  {
    label: "Instagram",
    href: "https://www.instagram.com/grupoasiaven?igsi=MW45ZjhjcGtrd2ViNQ==",
    path: "M2 7a5 5 0 0 1 5-5h10a5 5 0 0 1 5 5v10a5 5 0 0 1-5 5H7a5 5 0 0 1-5-5zm10 1.37a4 4 0 1 0 0 8 4 4 0 0 0 0-8zM17.5 6.5h.01",
  },
  {
    label: "TikTok",
    href: "https://www.tiktok.com/@grupoasiaven?_r=1&_t=ZS-99BZAPFjviT",
    path: "M9 12a4 4 0 1 0 4 4V4a5 5 0 0 0 5 5",
  },
  {
    label: "Facebook",
    href: "https://www.facebook.com/share/1DScmxRC2x/?mibextid=wwXIfr",
    path: "M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z",
  },
];

const corporateDescription = t(lang, "footer.description");

const quickLinks = [
  { label: t(lang, "nav.home"), href: getRouteHref("home", lang) },
  { label: t(lang, "nav.about"), href: `${getRouteHref("home", lang)}#nosotros` },
  { label: t(lang, "nav.services"), href: getRouteHref("services", lang) },
  { label: t(lang, "nav.projects"), href: getRouteHref("projects", lang) },
  { label: t(lang, "nav.contact"), href: getRouteHref("contact", lang) },
];

// Direcciones/teléfonos/correos son hechos, no se traducen; solo el nombre de la sede.
const offices = [
  {
    name: t(lang, "common.officeVenezuela"),
    items: [
      { Icon: Phone, text: "+58 212-9924333" },
      { Icon: Mail, text: "proyectos@asiaven.com" },
      { Icon: MapPin, text: "C.C Paseo Las Mercedes, Piso 2, Caracas, Venezuela" },
    ],
  },
  {
    // ⚠️ DATOS FICTICIOS/PROVISIONALES — confirmado por el dueño del proyecto.
    // El teléfono usa el prefijo 555 (reservado en Norteamérica para uso
    // ficticio) y la dirección tiene forma de placeholder. Reemplazar por los
    // datos reales de la oficina de Miami, o eliminar esta sede, antes de
    // salir a producción. Ver docs/ESTADO.md → BLOQUEANTES DE LANZAMIENTO.
    name: t(lang, "common.officeUnitedStates"),
    items: [
      { Icon: Phone, text: "+1 (305) 555-0198" },
      { Icon: Mail, text: "ventas@asiaven.com" },
      { Icon: MapPin, text: t(lang, "common.usOfficeAddress") },
    ],
  },
];

// Los documentos legales no se traducen (decisión registrada en docs/ESTADO.md):
// el label sí, el destino siempre es la versión española vinculante.
const legalLinks = [
  { label: t(lang, "footer.termsLabel"), href: getRouteHref("terms", lang) },
  { label: t(lang, "footer.privacyLabel"), href: getRouteHref("privacy", lang) },
];
---

<footer class="bg-corporativo-gray text-gray-300">
  <div class="mx-auto max-w-7xl px-6 py-16 lg:px-8">
    <div class="grid grid-cols-1 gap-12 md:grid-cols-2 lg:grid-cols-4">
      <div>
        <a href={getRouteHref("home", lang)} aria-label="ASIAVEN" class="inline-block text-white">
          <AsiavenLogo class="h-7" />
        </a>
        <p class="mt-4 max-w-xs text-sm leading-relaxed text-gray-400">{corporateDescription}</p>
        <div class="mt-6 flex items-center gap-4">
          {socialLinks.map(({ label, href, path }) => (
            <a
              href={href}
              aria-label={label}
              class="flex h-9 w-9 items-center justify-center rounded-full border border-white/15 text-gray-300 transition-colors duration-200 hover:border-white hover:text-white"
            >
              <svg
                class="h-4 w-4"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="1.5"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d={path} />
              </svg>
            </a>
          ))}
        </div>
      </div>

      <div>
        <h4 class="text-sm font-semibold uppercase tracking-widest text-white">{t(lang, "footer.quickLinksHeading")}</h4>
        <ul class="mt-4 flex flex-col gap-3">
          {quickLinks.map(({ label, href }) => (
            <li>
              <a href={href} class="text-sm text-gray-400 transition-colors duration-200 hover:text-white">
                {label}
              </a>
            </li>
          ))}
          {STORE_CATALOG_LIVE && (
            <li>
              <a
                href="/store"
                class="inline-flex items-center gap-1.5 text-sm font-semibold text-white transition-colors duration-200 hover:text-gray-300"
              >
                {t(lang, "footer.storeBridge")} &rarr;
              </a>
            </li>
          )}
        </ul>
      </div>

      <div>
        <h4 class="text-sm font-semibold uppercase tracking-widest text-white">{t(lang, "footer.contactHeading")}</h4>
        <div class="mt-4 flex flex-col gap-6">
          {offices.map((office) => (
            <div>
              <span class="mb-2 block text-xs font-semibold uppercase tracking-wide text-gray-300">
                {office.name}
              </span>
              <ul class="flex flex-col gap-3">
                {office.items.map(({ Icon, text }) => (
                  <li class="flex items-center gap-3 text-sm text-gray-400">
                    <Icon className="h-5 w-5 flex-shrink-0 text-gray-300" strokeWidth={1.5} />
                    <span>{text}</span>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>

      <div>
        <h4 class="text-sm font-semibold uppercase tracking-widest text-white">{t(lang, "footer.legalHeading")}</h4>
        <ul class="mt-4 flex flex-col gap-3">
          {legalLinks.map(({ label, href }) => (
            <li>
              <a href={href} class="text-sm text-gray-400 transition-colors duration-200 hover:text-white">
                {label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="mt-12 flex flex-col gap-4 border-t border-white/15 pt-8 sm:flex-row sm:items-center sm:justify-between">
      <p class="text-xs text-gray-500">&copy; {currentYear} {t(lang, "footer.copyrightSuffix")}</p>
      <ul class="flex items-center gap-6">
        {legalLinks.map(({ label, href }) => (
          <li>
            <a href={href} class="text-xs text-gray-500 transition-colors duration-200 hover:text-white">
              {label}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </div>
</footer>
